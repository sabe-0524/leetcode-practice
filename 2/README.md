問題文　https://leetcode.com/problems/add-two-numbers/submissions/1800201304/?source=submission-noac

step1
時間計算量；O(n) （nは桁数）ノード→数値、数値→ノードのいずれも各桁を一度だけ見ているから
空間計算量；O(n)　桁数分のノードを確保するから
一旦二つの連結リストを数値に直してからそれを足す方針にした
もしかしたらリストのまま足し算できる？step2で実装してみる
リストを数値にするのは二回繰り返すので関数化した

step2
計算量は変わらないが若干速くなっているはず
ただし、コードがかなり複雑になっている割にはそこまでの高速化ではないから普通は使わなくて良い気がする
個人的にはstep1の解法の方が良いんじゃないかと思う。ただし、今回は最大100桁だが、もっと大きい数字が入るケースが存在するならオーバーフロー処理に時間がかかるのを防ぐためにstep2の解法が良さそう
最後の繰り上げ処理は上手いことやれば消せそう