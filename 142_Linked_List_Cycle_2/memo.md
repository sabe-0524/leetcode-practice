リンクリストの循環検知メモ

単純に解くだけなら 141 と変わらない。

set を使って循環を検知したらそのノードを返すだけ。

計算量は 
𝑛
n 回探索して、その度に set の中から探しているから 
𝑛
log
⁡
𝑛
nlogn な気がする。

メモリを O(1) にする方法が難しい。

流石に毎回最初から探索するのが最適解であるとは思えない。

fast / slow（Floyd）での検討

前回やった fast / slow でやれば検知はできるかもしれない。

ただし、それだと衝突（meet）まで検知できても、衝突地点がサイクルの始まりである保証はない。

他の人の実装を参考に Floyd's cycle-finding algorithm を調べた。

衝突までの時間はサイクル長の整数倍になる。

初期化の問題（例）
fast = head.next
slow = head


このように初期化したが、これだと正常に動かない。

理由：fast と slow の位置が fast = 2 * slow の関係にならないから。